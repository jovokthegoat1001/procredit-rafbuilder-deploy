# dependencies
node_modules/

# build output
dist/
dist-ssr/

# secrets / environment
.env
.env.*
!.env.example
*.local

# sample / uploaded source documents — may contain real client data, and the app
# receives uploads at runtime, so these are never needed in the repo
uploads/

# local Claude Code / tooling state
.claude/

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS / editor cruft
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
